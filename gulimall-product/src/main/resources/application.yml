spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://121.196.36.170:3306/gulimall_pms?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
    driver-class-name: com.mysql.jdbc.Driver
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848


  application:
    name: gulimall-product
#  cloud:
#    nacos:
#      discovery:
#        server-addr: 127.0.0.1:8848
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
  #关闭thymeleaf缓存 开发期间就能看到实时效果；上线以后就要打开缓存了(效果不大)
  thymeleaf:
    cache: false
    #cache: true
  #配置Redis缓存
  redis:
    host: 121.196.36.170
    port: 6379

  session:
    store-type: redis


mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto
      logic-delete-value: 1
      logic-not-delete-value: 0
server:
  port: 10100
logging:
  level:
    com.atguigu.gulimall: debug